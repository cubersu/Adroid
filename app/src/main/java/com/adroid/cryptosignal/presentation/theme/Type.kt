package com.adroid.cryptosignal.presentation.theme

import androidx.compose.ui.text.font.FontFamily

/**
 * Used for every numeric readout (price, percent change, RSI, etc.) so figures stay aligned
 * and legible at a glance — a system generic monospace avoids bundling font assets.
 */
val MonoFontFamily = FontFamily.Monospace
